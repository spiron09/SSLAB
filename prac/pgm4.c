#include<stdio.h>
#include<string.h>
int i =0, k =0, j=0, z=0,c=0;
char a[20], ac[20], res[20], stk[20],act[20];
int main(){
    puts("THE GRAMMAR IS E->E+E E->E*E E->(E) E->id");
    puts("Enter String: ");
    scanf("%s", a);
    c = strlen(a);
    strcpy(act, "SHIFT->");
    puts("Stack\t input\t Action");

    for()

}

void check(){

}